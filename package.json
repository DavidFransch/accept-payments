{
  "name": "accept-payments-poc",
  "version": "1.0.0",
  "scripts": {
    "client": "npm run --prefix client start",
    "api": "npm run --prefix api start",
    "dev": "concurrently --kill-others-on-fail \"npm run api\" \"npm run client\""
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^8.2.0",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "concurrently": "^4.0.1",
    "nodemon": "^2.0.6"
  }
}
